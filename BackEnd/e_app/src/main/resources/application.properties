spring.application.name=e_app

# PostgreSQL connection. Override these values with environment variables when needed.
spring.datasource.url=jdbc:postgresql://localhost:5432/e_app
spring.datasource.username=postgres
spring.datasource.password=minhdz123
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.show-sql=true
